# ──────────────────────────────────────────────────────────────
# 环境变量模板（复制为 .env.local 本地使用，或填到 Vercel 后台）
#
# 方式一：本地开发用 Turso（可选）
#   复制本文件为 .env.local 并填入真实值即可。
#
# 方式二：Vercel 部署
#   在 Vercel 项目 Settings → Environment Variables 添加下面两个变量，
#   然后 Redeploy 使变量生效。
# ──────────────────────────────────────────────────────────────

# Turso（libSQL，Serverless 版 SQLite）连接地址
# 获取：turso db show <db-name> --url   （形如 libsql://xxx.turso.io）
TURSO_DATABASE_URL=

# Turso 访问令牌
# 获取：turso db tokens create <db-name>
TURSO_AUTH_TOKEN=
